Place your profile image here as `profile.jpg`.

Preferred filename: profile.jpg
Recommended size: 400x400 (square)
You can convert to WebP for better performance and rename to `profile.webp` — if you do, update the `src` in `index.html` accordingly.

If you want me to save and convert the uploaded image, re-attach it using the file upload button and I'll convert it and add it to this folder.